rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    match /users/{userId} {
      allow read, write: if request.auth != null && request.auth.uid == userId;

      match /parcelles/{parcelleId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }

      match /carnet/{activiteId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }

      match /diagnostics/{diagnosticId} {
        allow read, write: if request.auth != null && request.auth.uid == userId;
      }
    }
  }
}
